import React from 'react';
import {Header} from './assets/scss/Header.scss';

export default function(props) {
    return (
        <h1 className={Header}>
            SASS & SCSS
        </h1>
    );
}