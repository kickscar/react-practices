## ex01: Component - Property: Composing Component

### 01. Property(줄여서 Props)
1. 컴포넌트 조합(Component Composition)과 작성에 중요한 역할을 한다.
2. 부모 컴포넌트에서 자식 컴포넌트로 전달되는 데이터로 Data Flow(Top-Down)의 메카니즘을 제공한다. 
3. 자식 컴포넌트에서는 변경 불가(immutable)
4. 부모 컴포넌트가 전달하고 소유한다.

### 02. Food List src/01: Class Components
### 03. Food List src/02: Passing Data(Property) between Class Components
### 04. Food List src/03: Passing Data(Property) between Functional Components

### Run Examples
```bash
$ npm run debug src={no}
```


